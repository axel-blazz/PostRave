<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- tailwind cdn -->
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end w-full">
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2">Logout</a>
        </div>
        <h5 class="mb-5">Edit your post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea class="resize-none bg-transparent border-2 border-zinc-800 rounded-md w-1/3 p-3 outline-none block mb-3" name="content"><%= post.content %></textarea>
            <button class="px-5 py-2 bg-yellow-500 rounded-md block text-black" type="submit">Update Post</button>
        </form>
    </div>
</body>
</html>